# Roadmap

## Now

- Keep expanding Darija semantic mappings.
- Run 100+ trust cases after every RAG change.
- Add failed real user questions to `eval_cases.jsonl`.
- Keep deterministic answers for high-risk legal topics.

## Next

- Add `/health` preflight in Flutter.
- Add settings sheet for local/LAN/ngrok API switching.
- Improve CNSS and accident answers with stronger source documents.
- Add latency and retrieval relevance reporting.

## Later

- Build 500+ clean training examples.
- Keep `qwen2.5:7b` as the default chat model and compare `llama3.1:8b` only in eval runs.
- Consider QLoRA only after the dataset and evaluation are stable.
- Deploy to VPS with HTTPS, API key, logs, and rate limiting.
